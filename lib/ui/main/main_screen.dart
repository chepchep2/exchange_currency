import 'package:flutter/material.dart';

class ExchangeCurrencyMainScreen extends StatelessWidget {
  const ExchangeCurrencyMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
    ),
    );}}